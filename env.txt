# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For local: mongodb://localhost:27017/watchverse
# For production (MongoDB Atlas): mongodb+srv://<username>:<password>@cluster.mongodb.net/watchverse
MONGO_URI=mongodb://localhost:27017/watchverse

# Security
# Generate a strong secret for production
JWT_SECRET=your_super_secret_key_123_change_this_in_production
